// Client-side Supabase Helper (Shared with frontend)
// Mas aqui vamos focar em helpers específicos se precisar
// Como já temos o supabaseClient global no app.js, esse arquivo pode ser um wrapper
// ou apenas conter configurações extras.

// Se o usuário quiser usar o node client no frontend (via browserify/webpack), seria útil.
// Mas como é HTML puro + CDN, vamos usar o window.supabaseClient do app.js.

console.log('supabaseClient.js carregado (Placeholder para lógica extra)');
